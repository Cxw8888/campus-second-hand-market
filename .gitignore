# 前端
frontend/node_modules/
frontend/dist/
frontend/.npm-cache/

# 用户上传的图片（批次文档修复批 · 自审 Minor 20）
# 结构：用户图落在 uploads/product/{userId}/{uuid}.jpg（LocalStorageImpl 落盘规则），
# 而仓库根 uploads/ 下的 demo-*.png 是演示数据、必须继续跟踪 → 只忽略子目录，不忽略根文件。
# 背景：uploads/ 此前未被忽略，用户上传的真实图片（可能含 PII）会被提交进公开仓库。
/uploads/product/
/uploads/tmp/
/uploads/*.tmp

# 后端
target/
*.class
*.log

# IDE
.idea/
*.iml
.vscode/

# 环境变量文件（批次 6.0.6 · 自审 Minor 20 残留）
# 当前仓库没有 .env，但敏感值（PAY_CALLBACK_SECRET / JWT_SECRET / DB_PASSWORD / API key）
# 将来很可能落在 .env* 里 —— 一旦被 commit 就会推到公开仓库，故默认全部忽略。
# 例外：.env.example 是**不含真实值的模板**，必须能被跟踪（`!` 取反要放在忽略规则之后）。
.env
.env.*
!.env.example

# 系统
.DS_Store
Thumbs.db