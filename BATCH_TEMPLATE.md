# 每批用户消息模板

开新会话时，把下面两块作为用户消息发送（System Prompt 已贴 SYSTEM_PROMPT.md）。

---

【当前状态】
- src/ 文件数：（每批结束填实际数字）
- 路由数：（每批结束填实际数字）
- 单测：（每批结束填「X 文件 / Y 用例全绿」）
- 已完成批次：（递增）
- 上一批遗留待办：（只列真正需要下一批处理的项，没有就写「无」）
- 本批定位：（如果大批次拆了子批，注明是 5.1 / 5.2 / 5.3）
- 环境：（后端 8080 是否已启动 / 管理员账号 role 是否已提权 / 有无浏览器可实测；没有的写「无」，本批报告「五、没能验证的部分」据此写）

【本批需求】
[在这里写本批要做什么，具体到页面 / 功能级别，避免「继续开发」这种模糊指令]

---

# 字段说明

| 字段 | 更新时机 | 说明 |
|---|---|---|
| src/ 文件数 | 每批结束 | 报告里的实际数字 |
| 路由数 | 每批结束 | router/index.js 中实际定义数 |
| 单测 | 每批结束 | npm test 输出的文件数 / 用例数 |
| 已完成批次 | 每批结束 | 递增 |
| 上一批遗留待办 | 每批结束 | 只列真正需要下一批处理的项，没有就写「无」 |
| 本批定位 | 每次填写 | 大批次拆子批时注明，如「第五批子批 5.1」 |
| 环境 | 每次填写 | 后端 / 管理员账号 / 浏览器这三项的可验证状态，决定本批能做多少真机验证 |

# 注意

- 不要把全量文件清单塞进状态摘要——那是报告的事
- 不要写「最近一批重点」——5 个数字 + 遗留待办已经足够
- 本批需求尽量具体到页面 / 功能级别，避免「继续开发」这种模糊指令
- 如果是子批（如 5.1），注明本批范围 + 后续子批范围，让 Agent 知道节奏
- 每批收尾必须走一遍 SYSTEM_PROMPT.md 的「每批文档同步检查」：改了什么就同步对应文档，没有改动就明确写「无需修改」，并在报告「一、文件清单」里列出文档改动
